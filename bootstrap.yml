- name: Include mailgun secrets
  hosts: external_servivces
  become: yes
  tasks:
    - name: Include vaulted vars
      include_vars:
        file:  secrets/mailgun/vars.yml
  tags: 
    - bootstrap_fail2ban

- name: Set-up baseline for VM
  hosts: external_servivces
  become: yes
  tasks:
    - name: Include vaulted vars
      include_vars:
        file:  secrets/mailgun/vars.yml
  roles:
    - fail2ban
  tags: 
    - bootstrap_fail2ban